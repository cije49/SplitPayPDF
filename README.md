# SplitPayPDF

SplitPayPDF is a Windows desktop application for splitting, managing, and processing PDF files.  
It features a clean ttkbootstrap UI, helpful popups, and multiple PDF utilities built into a single tool.

## ✨ Features

### **PDF Splitter**
- Split PDFs by:
  - Page ranges  
  - Every N pages  
  - Specific lists of pages  
- Auto-rename duplicate output files  
- Intuitive progress messages and dialogs  
- Support for Unicode file paths

### **PDF Tools**
- PDF → CSV extraction using PyMuPDF + pandas  
- Page counting  
- PDF metadata reader  
- File utilities  
- Clean UI with help popups

## 🛠️ Requirements

Install dependencies:

```bash
pip install -r requirements.txt
